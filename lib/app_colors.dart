import 'package:flutter/material.dart';

class AppColors {
  static Dark dark = Dark();
  static ConstatelColors constatel = ConstatelColors();
}

class ConstatelColors {
  Color blue = const Color(0xFF175CDC);
  Color red = Colors.red;
}

class Dark {
  Color black = const Color(0xff191720);
}